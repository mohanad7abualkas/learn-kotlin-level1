fun main (arg : Array<String>){

    // الـ Char نوع من أنواع الـ DataType يخزن أحرف فقط
    var a: Char = 'a'
    println(a.toUpperCase())

    // هذه الدالة لتحويل الحرف الصغير إلى حرف كبير toUpperCase()

    println(a.lowercase())
}